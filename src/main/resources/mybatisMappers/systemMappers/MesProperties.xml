<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vtest.it.dao.systemdao.MesPropertiesDAO">
    <select id="getProperties" resultType="properties">
      SELECT host,init_url,acode FROM rawdatainfors.mesconfig;
    </select>
    <update id="updateProperties" parameterType="properties">
      UPDATE rawdatainfors.mesconfig SET host=#{host},init_url=#{initUrl},acode=#{acode} WHERE status=TRUE ;
    </update>
</mapper>